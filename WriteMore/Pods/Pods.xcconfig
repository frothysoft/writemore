GCC_PREPROCESSOR_DEFINITIONS = $(inherited) COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Mixpanel-OSX-Community"
OTHER_CFLAGS = $(inherited) -isystem "${PODS_ROOT}/Headers" -isystem "${PODS_ROOT}/Headers/Mixpanel-OSX-Community"
OTHER_LDFLAGS = -ObjC -framework Cocoa -framework Foundation -framework SystemConfiguration
PODS_ROOT = ${SRCROOT}/Pods